services:
  api:
    build: ./services/api
    ports:
      - '5000:5000'
    env_file:
      - ./services/api/.env
  web:
    build: ./apps/web
    ports:
      - '3000:3000'
    depends_on:
      - api
